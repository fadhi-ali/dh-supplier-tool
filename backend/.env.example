# === Database ===
DATABASE_URL=postgresql+asyncpg://supplier_user:supplier_pass@localhost:5432/supplier_portal

# === Security ===
SECRET_KEY=change-me-in-production
ADMIN_PASSWORD=doorbell-ops-2024

# === Anthropic (AI chat assistant + catalog extraction) ===
# Required for /chat endpoint and catalog processing. Returns 503 if empty.
ANTHROPIC_API_KEY=sk-ant-...

# === Stripe (payment processing, Tier 1 only) ===
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# === Resend (email delivery for magic links + notifications) ===
# Without RESEND_API_KEY, emails are logged to the console only.
# Sign up at https://resend.com and verify your sending domain.
RESEND_API_KEY=re_...
FROM_EMAIL=onboarding@resend.dev

# === Frontend URL (used in email links) ===
FRONTEND_URL=http://localhost:3000
